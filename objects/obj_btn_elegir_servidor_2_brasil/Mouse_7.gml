/// @description Va a la pantalla previa a jugar
if ( global.menuBloq == true){
	escala=1
	
	/// sonido
	if global.musica == true
	{ audio_play_sound(snd_boton,100,false) }

	// Se califica el juego
	global.ipServidor = global.servidor2Brasil 
	
}
